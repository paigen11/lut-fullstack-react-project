import Layout from '../../components/Layout';

const Event = () => {
  // how to do a folder-level named page i.e. /event
  return (
    <Layout>
      <h1>Event</h1>
    </Layout>
  );
};

export default Event;
